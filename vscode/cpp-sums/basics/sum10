Single File Programming Question
Problem Statement :
Round Off
Write a program to get a float value from the user and display it in the below-mentioned format. HINT: Use ceil() and floor() functions from header file cmath.

Input format :
Input consists of one float value.

Output format :
The output consists of one integer, its highest round-off value, and its lowest round-off value.

Sample test cases :
Input 1 :
54.5

Output 1 :
54
55
54

Input 2 :
32.66

Output 2 :
32
33
32


// You are using GCC
#include <iostream>
#include <cmath>

using namespace std;

int main() {
    float value;
    cin >> value;
    
    int inte = value;
    int highest_round_off = ceil(value);
    int lowest_round_off = floor(value);
    
    cout << inte << endl;
    cout << highest_round_off << endl;
    cout << lowest_round_off << endl;
    
    return 0;
}
